@page "/"
@inject IProductServiceInterface ProductService
<PageTitle>TM Eshop</PageTitle>

<Featured />

@code 
{
    [Parameter]
    public string? CatUrl { get; set; } = null;

    protected override async Task OnParametersSetAsync()
    {
            await ProductService.GetProducts(CatUrl);
    }
}
